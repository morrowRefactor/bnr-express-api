CREATE TABLE vid_resources (
    id INTEGER PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY,
    description TEXT NOT NULL,
    link TEXT NOT NULL,
    vid INTEGER
        REFERENCES videos(id) ON DELETE CASCADE NOT NULL
);